import styled from 'styled-components';

const HighlighterWrapper = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  z-index: 9000;
  cursor: url(highlighter.png) 8 64, auto;
`;

export default { HighlighterWrapper };
